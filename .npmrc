# pnpm config for Netlify builds.
#
# Hoist workspace packages into a flat node_modules so
# @netlify/plugin-nextjs and Next can resolve @recalllens/*
# via standard Node resolution (not pnpm's symlink-per-package layout).
public-hoist-pattern[]=*
shamefully-hoist=true